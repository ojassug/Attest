# Runtime dependencies for the AgentCore Runtime image.
# Pinned deliberately: the deployed runtime must behave exactly as the demo video shows.
# Regenerate with the snippet in docs/aws-setup.md; streamlit is intentionally absent -
# the UI is not part of the runtime image.
strands-agents==1.54.0
strands-agents-tools==0.8.8
bedrock-agentcore==1.22.0
pydantic==2.13.5
pyyaml==6.0.3
fpdf2==2.8.8
boto3==1.43.89
